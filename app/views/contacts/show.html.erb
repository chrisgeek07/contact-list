<div class="container">
  <div class="contact-card">
    <h3><%= @contact.name%></h3>
    <p><%= @contact.address%></p>
    <p><%= @contact.country%></p>
    <p><%= @contact.phone%></p>
  </div>
</div>
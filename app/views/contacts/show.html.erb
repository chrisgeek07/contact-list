<div class="container">
  <div class="contact-card mt-4">
    <h4><%= @contact.name%></h4>
    <p><%= @contact.address%></p>
    <p><%= @contact.country%></p>
    <p><%= @contact.phone%></p>
  </div>
</div>